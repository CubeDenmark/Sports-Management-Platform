import { AdminDestination } from '@/components/admin-destination'
export default function SchedulePage() { return <AdminDestination eyebrow="Workspace" title="Schedule" description="Build the matchday schedule, assign courts, and monitor match status." /> }
