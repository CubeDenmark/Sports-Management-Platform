import { AdminDestination } from '@/components/admin-destination'
export default function SettingsPage() { return <AdminDestination eyebrow="System" title="Settings" description="Configure operational defaults and platform behavior. Secrets and credentials stay outside this interface." /> }
